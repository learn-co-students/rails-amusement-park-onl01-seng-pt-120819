<h1>Login</h1>


<%= form_for signin_path do |f| %>
<%= f.label :name, "Your name:" %>
<%= collection_select :user, :user_id, User.all, :id, :name, id: "user_name" %>

<%= f.label :password %>
<%= f.password_field :password %>

<%= f.submit "Sign in" %>

<%end%>
